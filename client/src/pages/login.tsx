//Default landing page if not logged in

export default function Login(){
    //placeholder until actual login page can be written
    return(
        <section>
            <h2>Log In</h2>
            <p>This is all placeholder text until the actual page is complete</p>
        </section>
    )
}

